{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Author merge</h1>
{% include '::flashmessage.html.twig' %}
    <form action="{{ path('matuck_library_author_merge') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Merge</button>
        </p>
    </form>
{% endblock %}
