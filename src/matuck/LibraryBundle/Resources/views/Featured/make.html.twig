{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Featured creation</h1>
    {% include '::flashmessage.html.twig' %}
    <form action="{{ path('matuck_library_featured_make', { 'book': book.getId()}) }}" method="POST" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        {{form_rest(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('matuck_library_book_show', {'id': book.getId()}) }}">
            Back to Book
        </a>
    </li>
</ul>
{% endblock %}
