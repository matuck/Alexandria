{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Flags creation</h1>
    {% include '::flashmessage.html.twig' %}
    <form action="{{ path('matuck_library_flags_create', { 'id': bookid}) }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('matuck_library_book_show', {'id': bookid}) }}">
            Back to Book
        </a>
    </li>
</ul>
{% endblock %}
